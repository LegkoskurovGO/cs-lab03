#ifndef inputing_hpp
#define inputing_hpp

#include <stdio.h>
#include <iostream>
#include <vector>
#include <sstream>
using namespace std;

vector<double> input_numbers(size_t count, stringstream& stream);
vector<double> input_numbers(size_t count);

#endif /* inputing_hpp */
